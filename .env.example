# Copy this file to .env and fill in your values.
# .env is gitignored — never commit your actual keys.

# Gemini API key — required for the OLX Gemini parser toggle in the UI.
# Get one for free at https://aistudio.google.com/app/apikey
GEMINI_API_KEY=
